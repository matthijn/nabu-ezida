{
  "files": {},
  "args": { "commands": ["if true"] },
  "expected": {
    "status": "partial",
    "output": [{ "stdout": "", "stderr": "This is not bash. 'if' is not supported.\nUse only: cat, head, tail, ls, grep, find, wc, cut, sort, uniq, tr, sed, echo, cp, mv, rm, touch", "outcome": { "type": "exit", "exit_code": 1 } }],
    "mutations": []
  }
}
